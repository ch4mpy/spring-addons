server:
  port: 8090
  
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/auth/realms/master

showcase:
  jpa: false
  management:
    username: actuator
    password: secret
    
com.c4-soft.springaddons.test.web:
  default-media-type: application/json
  default-charset: utf-8